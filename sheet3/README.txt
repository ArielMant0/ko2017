Aufgabe P3

b)
Für die erste Variante muss in der Funktion "solve(instance)" der
Klasse "ChocoSolverB" die Zeile "return withKnapsack(instance)"
ausgeführt werden. Die andere Variante wird an der gleichen Stelle
durch die Zeile "return withoutKnapsack(instance)" ausgeführt.